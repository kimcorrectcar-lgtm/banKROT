services:
  - type: web
    name: bankrot-bot
    runtime: python
    buildCommand: python3 -m pip install -r requirements.txt
    startCommand: python3 main.py

    envVars:
      - key: PYTHON_VERSION
        value: 3.11.11

      - key: PORT
        value: 10000
